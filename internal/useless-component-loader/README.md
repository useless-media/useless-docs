# Useless Component Loader

The [ComponentLoader](reference/classes/ComponentLoader) is a universal component loader designed to work in both a node and a browser environment.

It takes a component load configuration which specifies how to load/install the component and how to extract the desired
exports from what is loaded.