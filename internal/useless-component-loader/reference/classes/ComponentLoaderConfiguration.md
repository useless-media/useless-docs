# ComponentLoaderConfiguration

## Constructors

### Constructor

> **new ComponentLoaderConfiguration**(): `ComponentLoaderConfiguration`

#### Returns

`ComponentLoaderConfiguration`

## Properties

### exec()?

> **exec**?: (`command`) => `Promise`<{ `stderr`: `string`; `stdout`: `string`; }>

#### Parameters

##### command

`string`

#### Returns

`Promise`<{ `stderr`: `string`; `stdout`: `string`; }>
