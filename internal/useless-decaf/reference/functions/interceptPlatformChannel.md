# interceptPlatformChannel

> **interceptPlatformChannel**(`messageHandler`, `sendHandler`): `any`

## Parameters

### messageHandler

(`message`) => `Promise`<`boolean`>

### sendHandler

(`message`) => `Promise`<`boolean`>

## Returns

`any`
