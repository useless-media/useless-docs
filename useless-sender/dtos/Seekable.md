<div class="useless-tab-container">

- TypeScript

  ```ts
  interface Seekable {
    end: number;
    start: number;
  }
  ```

- Kotlin

  ```kotlin
  data class Seekable(
    var end: Double,
    var start: Double
  )
  ```

</div>